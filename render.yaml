services:
  - type: web
    name: ytdlp-frontend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: YTDLP_API_BASE_URL
        value: http://ytdlp56.duckdns.org:5000
